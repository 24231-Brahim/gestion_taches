<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">
    <changeSet id="20260722000000-1" author="brahim">
        <comment>Revert TaskStatus: IN_REVIEW → READY_FOR_TEST, TODO → NEW, CANCELLED → DONE</comment>
        <sql dbms="postgresql">
            UPDATE task SET status = 'READY_FOR_TEST' WHERE status = 'IN_REVIEW';
            UPDATE task SET status = 'NEW' WHERE status = 'TODO';
            UPDATE task SET status = 'DONE' WHERE status = 'CANCELLED';
        </sql>
        <sql dbms="h2">
            UPDATE task SET status = 'READY_FOR_TEST' WHERE status = 'IN_REVIEW';
            UPDATE task SET status = 'NEW' WHERE status = 'TODO';
            UPDATE task SET status = 'DONE' WHERE status = 'CANCELLED';
        </sql>
    </changeSet>
</databaseChangeLog>
